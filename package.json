{
  "name": "communa",
  "version": "1.0.0",
  "private": true,
  "description": "Root-level convenience scripts. The actual app lives in backend/ and frontend/ — this just runs both together for local development.",
  "scripts": {
    "dev": "concurrently -n backend,frontend -c blue,green \"npm --prefix backend run dev\" \"npm --prefix frontend run dev\"",
    "install:all": "npm install --prefix backend && npm install --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
